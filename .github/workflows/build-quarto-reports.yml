name: Build and Deploy GitHub Pages

on:
  push:
    branches: [ main ]
    paths:
      - 'reports/**/*.qmd'
      - 'index.qmd'
      - '_quarto.yml'
      - 'sources/**'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build Work Guide
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Setup Quarto
      uses: quarto-dev/quarto-actions/setup@v2

    - name: Install LaTeX and fonts for PDF generation
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          texlive-latex-base \
          texlive-latex-recommended \
          texlive-latex-extra \
          texlive-fonts-recommended \
          texlive-fonts-extra \
          texlive-xetex \
          texlive-luatex \
          texlive-lang-japanese \
          texlive-lang-cjk \
          fonts-noto-cjk \
          fonts-noto-cjk-extra \
          librsvg2-bin

    - name: Verify Quarto installation
      run: |
        quarto --version
        quarto check

    - name: Build website
      run: |
        echo "📚 Building work-fundamentals website..."
        quarto render
        echo "✅ Build complete"

    - name: List output
      run: |
        echo "Generated files:"
        ls -la output/ || echo "No output directory"
        find output/ -type f | head -30 || true

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: output/

    - name: Generate build summary
      run: |
        echo "## 📊 Build Results" >> $GITHUB_STEP_SUMMARY
        echo "- Status: ✅ Success" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📦 Generated Content" >> $GITHUB_STEP_SUMMARY
        find output/ -type f \( -name "*.html" -o -name "*.pdf" -o -name "*.docx" \) | while read file; do
          echo "  - $file" >> $GITHUB_STEP_SUMMARY
        done

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4

    - name: Generate deployment summary
      run: |
        echo "## 🚀 Deployment Results" >> $GITHUB_STEP_SUMMARY
        echo "- Status: ✅ Success" >> $GITHUB_STEP_SUMMARY
        echo "- URL: ${{ steps.deployment.outputs.page_url }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📦 Content" >> $GITHUB_STEP_SUMMARY
        echo "- Top page: index.html" >> $GITHUB_STEP_SUMMARY
        echo "- Work Guide: reports/work-guide.html" >> $GITHUB_STEP_SUMMARY
        echo "- Downloads: work-guide.pdf, work-guide.docx" >> $GITHUB_STEP_SUMMARY
